apiVersion: v1
kind: Service
metadata:
  name: mock-api
  namespace: uc3-applications
  labels:
    app: mock-api
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: mock-api
