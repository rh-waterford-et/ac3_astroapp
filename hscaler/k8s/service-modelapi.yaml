apiVersion: v1
kind: Service
metadata:
  name: model-api
  namespace: uc3-applications
  labels:
    app: model-api
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: model-api
