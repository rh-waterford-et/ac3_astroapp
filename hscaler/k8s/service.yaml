apiVersion: v1
kind: Service
metadata:
  name: predictor-client
  namespace: uc3-applications
  labels:
    app: predictor-client
spec:
  type: ClusterIP
  ports:
  - port: 9090
    targetPort: metrics
    protocol: TCP
    name: metrics
  selector:
    app: predictor-client
